# 2026-08-16

## UI redesign 設計方案（待確認後實作）
- 方向：Claude 式復古奶油風，替換現有深色主題（src/styles.css + client.tsx 的 Mark 組件）
- 色板：bg #FAF8F3 / panel #F1EDE3 / surface #FFF / ink #29261F / muted #75705F / line #E5E0D3 / accent 赤陶 #D97757（hover #C4643F，soft #F6E3D9）/ ok #6B8F5E / run #B08D3E / err #B0544A
- 字體：Fraunces（標題）+ Source Serif 4（正文，中文回退 Noto Serif TC）+ Inter（UI 功能字）+ IBM Plex Mono（chip），均 OFL，Google Fonts 引入
- Logo：赤陶 8 瓣星芒 asterisk 替換現有六邊形 Mark
- 組件：agent 回覆無氣泡（留白分層）、用戶氣泡白底細邊、輸入框聚焦赤陶邊+soft 外暈、側欄 active 左豎條
- 待用戶確認的決策點：正文字體（Source Serif 4 vs Inter）、是否保留深色模式、Logo 星芒 vs 其他

## 實作完成（同日）
- 用戶決策：正文 Source Serif 4；需深色模式；不要圖形 Logo（只留文字站名 "edge agent"）；側欄完全平鋪無按鈕框；輸入框 focus 去掉粗外暈只留邊框變色；輸入框圓角 24→16px
- 改動：index.html 加 Google Fonts（Fraunces/Source Serif 4/Inter/IBM Plex Mono）；styles.css 全量重寫（亮/暗雙主題，暗色用 prefers-color-scheme，暖棕 #1F1C18 底非純黑）；client.tsx 刪除 Mark 組件改 wordmark 文字、New chat 併入 side-list
- 深色色板：bg #1F1C18 / panel #26221D / surface #2D2923 / ink #E9E3D6 / muted #9C9482 / line #3C372E / accent 同 #D97757（hover #E08A6C）
- 既有問題：tsc 報 agent.ts/server.ts 缺 Env 類型（需跑 npm run types 生成，與本次無關）
- 追加微調：全域滾動條平時隱藏、hover 元素時顯示 6px 細條（webkit + Firefox scrollbar-width）；側欄底部文案改為 "Cloudflare Edge"（去掉 GBrain）
- 已 commit cb3f07a 並 push 到 github.com/limerencel/cf-think-agent（master）；GCM 認證需 GCM_INTERACTIVE=always 觸發 GUI
- 用戶氣泡再優化：去掉 border 和 box-shadow，新增 --bubble token（light #F1EDE3 同側欄色 / dark #2E2A24），純背景色區分，消除與 AI 回覆的高度差；--shadow-warm token 已移除
- 觀察：AI 回覆的 markdown 未渲染（**、反引號原樣顯示），textOf 直接拼接 text part；用戶尚未要求處理

## Markdown 渲染設計方案（已提議，待用戶確認）
- 技術：react-markdown + remark-gfm + rehype-highlight（highlight.js core 按需語言）；Source Serif 4 需補 600 字重
- 代碼塊「深色島嶼」：明暗兩態統一暖黑底 #29261F，頂欄左語言標籤右 Copy/Copied 按鈕，圓角 12px
- 高亮 5 色：base #E9E3D6 / keyword #E08A6C / string #A9C48A / number #C9A962 / comment #8A8271 italic / function #8FB8C9
- 基礎元素：赤陶圓點 bullet、ol 數字 muted、行內碼 accent-soft 底、引用左 2px accent 豎條、表格 panel 底表頭
- 節奏：段落下距 12px、代碼塊上下 14px、標題 18/8、列表項距 4px

## Markdown 實作 + 側欄交互重做（同日）
- Markdown 已實作：react-markdown + remark-gfm + rehype-highlight；CodeBlock 組件（頂欄語言標籤 + Copy/Copied 按鈕，clipboard API + execCommand 回退）；AI 消息走 Markdown，用戶消息純文本
- 教訓：沙盒中跑 vite dev 會觸發 safe-delete 攔截（94 文件刪除提示）與 wrangler AppData EPERM；殺掉殘留 node 進程 + 刪 .wrangler 注册表文件 + shm/wal 鎖文件後可用非沙盒模式啟動。用戶對頻繁刪文件審批反感，以後 dev server 盡量讓用戶自己起
- 側欄重做：聊天頁頂部 wordmark 刪除；toggle 按鈕放右上角（chat header 右側 + home 右上角）；sideOpen 狀態桌面默認展開（matchMedia 900px）；桌面為推擠動畫（shell padding-left transition），移動端浮層無暗化 scrim（已刪），改 box-shadow 分隔
- hover 跳動修復：.side-del 從 display:none/grid 改為 opacity/visibility（佈局恆定）；.side-list 加 flex column gap 2px
- 用戶自己在期間提交了 2 個 commit（Parallel MCP search、AIG metadata tag）
- 後續追加：textarea 滾動條永久隱藏；側欄頭部右上加收起 icon（SideCollapseIcon）；收起後留 52px 細軌道 rail（桌面端）可再展開；rail 入場動畫從滑入改淡入（滑入看起來像反彈）
- 已 commit 4e53d8a 並 push（GCM_INTERACTIVE=always 需重試一次才彈認證）
